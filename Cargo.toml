[package]
name = "static-gateway"
version = "0.2.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
description = "A small Host-to-S3 static website gateway for personal K3s clusters"

[dependencies]
axum = "0.8"
percent-encoding = "2.3"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "stream"] }
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread", "signal", "time"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
url = "2"
serde_json = "1"
chrono = "0.4"
pulldown-cmark = "0.13"
ammonia = "4"
sha2 = "0.10"
regex = "1"

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
